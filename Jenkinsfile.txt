pipeline {
    agent any

    tools {
        jdk 'JDK17'
    }

    stages {
        stage('Build') {
            steps {
                // Сборка проекта с помощью Maven или Gradle
                sh './gradlew build' // Если вы используете Gradle
            }
        }

        stage('Test') {
            steps {
                // Запуск тестов
                sh './gradlew test' // Если вы используете Gradle
            }
        }

        stage('Deploy') {
            steps {
                // Шаги для развертывания (если требуется)
                echo 'Deploying application...'
            }
        }
    }

    post {
        success {
            echo 'Build succeeded!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}